<%= form_with(model: score_item, local: true) do |form| %>
  <% if score_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(score_item.errors.count, "error") %> prohibited this score_item from being saved:</h2>

      <ul>
      <% score_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :score_item_name %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :score_item_number %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.text_field :score, id: :score_item_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
